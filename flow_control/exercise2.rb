def allcaps(string)
  if string.length > 10
    puts string.upcase
  end
end
  
  allcaps("Hello World")
  
  #SOLUTION: The same as above except solution has 
  # else -> string 
  #also, solution doesn't have puts in method but instead
  #when calling the method